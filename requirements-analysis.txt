# Optional heavy dependencies for Tier 3 local audio analysis.
# Without these the tool still works — it just skips the local fallback
# (equivalent to always running with --api-only).
yt-dlp>=2024.4.9
librosa>=0.10
numpy>=1.24
